## FretMate ReadMe

# Tests

npm install --legacy-peer-deps
npm test


# Running it

npm install --legacy-peer-deps
npx expo start

# How to open the App

Then once its all loaded you can press "I" to simulated the app on a iPhone or
you can press "A" to simulated the app on a android.

You can also scan the QR code that appears but you will have to download the expo app and go from there.

